{
  "name": "one-click-email-automation",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for preparing repetitive email outreach.",
  "scripts": {
    "test": "node --test test/utils.test.js",
    "check": "node --check src/background.js && node --check src/options.js && node --check src/utils.js && node --check src/google.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest.json: OK')\"",
    "validate": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
