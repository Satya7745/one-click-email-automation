{
  "name": "one-click-email-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for preparing repetitive email outreach.",
  "scripts": {
    "test": "node --test test/utils.test.js",
    "check": "node --check src/background.js && node --check src/options.js && node --check src/utils.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8')); console.log('manifest.json: OK')\"
  },
  "engines": {
    "node": ">=20"
  }
}
